title = "hydra-arms gitleaks config"

[extend]
  useDefault = true

[allowlist]
  description = "Files with known or expected high-entropy strings — not real credentials"
  paths = [
    '''supabase/migrations/004_xml_import_fields\.sql''',
    '''xml-integration/samples/''',
  ]
